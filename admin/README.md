# HabitLine Admin Dashboard (Vite + React)

HabitLineの管理画面 - Vite + React + TypeScript + Tailwind CSS

## ✅ 技術スタック

- ⚡ **Vite 5** - 高速ビルドツール
- ⚛️ **React 18** - UIライブラリ
- 📘 **TypeScript** - 型安全
- 🎨 **Tailwind CSS** - ユーティリティファーストCSS
- 🗄️ **Supabase** - バックエンド
- 🛣️ **React Router** - ルーティング

## 🚀 セットアップ

### 環境変数の設定

```bash
cp .env.example .env
```

`.env` を編集:

```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

### 依存関係のインストール

```bash
npm install
```

### 開発サーバーの起動

```bash
npm run dev
```

**デフォルトポート**: 8080
**ポート自動調整**: 8080が使用中の場合、自動で次のポート(8081, 8082...)を使用

http://localhost:8080 でアクセス

## 📦 ビルド

```bash
npm run build
```

成果物: `dist/` ディレクトリ

### プレビュー

```bash
npm run preview
```

本番ビルドをローカルで確認（ポート8080）

## 🌐 デプロイ (Netlify)

### 自動デプロイ

1. **GitHubにプッシュ**
   ```bash
   git push origin main
   ```

2. **Netlifyで設定** (初回のみ)
   - Import from GitHub
   - 設定は `netlify.toml` で自動検出
   - 環境変数を追加:
     - `VITE_SUPABASE_URL`
     - `VITE_SUPABASE_ANON_KEY`

3. **以降は自動デプロイ**
   - `git push` でビルド & デプロイ

## 📁 プロジェクト構造

```
admin/
├── index.html              # エントリーHTML
├── vite.config.ts          # Vite設定
├── tsconfig.json          # TypeScript設定
├── package.json
└── src/
    ├── main.tsx           # エントリーポイント
    ├── App.tsx            # ルート設定
    ├── index.css          # グローバルスタイル
    ├── vite-env.d.ts      # Vite型定義
    ├── pages/
    │   ├── Home.tsx       # トップページ
    │   └── Dashboard.tsx  # ダッシュボード
    ├── lib/
    │   └── supabase.ts    # Supabaseクライアント
    └── types/
        └── database.ts    # DB型定義
```

## ⚙️ スクリプト

| コマンド | 説明 |
|---------|------|
| `npm run dev` | 開発サーバー起動（ポート8080） |
| `npm run build` | 本番ビルド |
| `npm run preview` | ビルドをプレビュー（ポート8080） |
| `npm run lint` | ESLint実行 |
| `npm run type-check` | TypeScriptチェック |

## 🔧 ポート設定

Viteは8080ポートから開始し、使用中なら自動で次のポートを試します:
- `strictPort: false` - ポート自動調整
- 8080 → 8081 → 8082 → ...

---

**HabitLine** - 続ける力を、設計で支える。
